fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 (sin mantenimiento desde 2020) usa `bcrypt.__about__` para
# detectar su versión y aplica un workaround de compatibilidad que bcrypt
# >=4.1 eliminó, rompiendo hash/verify con `ValueError: password cannot be
# longer than 72 bytes` incluso con passwords cortas. Pinneado hasta migrar
# a `bcrypt` directo o `pwdlib`.
bcrypt<4.1
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0
mercadopago>=2.2.0
